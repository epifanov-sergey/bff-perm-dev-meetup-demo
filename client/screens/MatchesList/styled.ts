import styled from 'styled-components';
import { Card, List } from 'antd';

export const ListWithWidth = styled(List)`
  width: 800px;
`;

export const GreenCard = styled(Card)`
  background: #ccf5d6;
`;

export const RedCard = styled(Card)`
  background: #fff1f0;
`;
